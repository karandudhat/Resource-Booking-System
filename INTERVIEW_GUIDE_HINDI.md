# 🚀 Full-Stack Resource Booking System — Detailed Interview Guide (Hindi)

यह डॉक्यूमेंट पूरे प्रोजेक्ट का **Deep Dive** (गहराई से विश्लेषण) है। इंटरव्यूअर आपसे आर्किटेक्चर और आपके द्वारा लिए गए टेक्निकल फैसलों (Technical Decisions) के बारे में सवाल पूछेगा। आपको इसे ध्यान से पढ़ना है ताकि आप हर लाइन का लॉजिक समझा सकें।

---

## 🏗️ 1. Project Architecture (प्रोजेक्ट कैसे काम करता है?)
हमारा प्रोजेक्ट **3-Tier Architecture** पर आधारित है:
1. **Frontend (Client):** React.js + TypeScript (UI और API calling के लिए)
2. **Backend (Server):** NestJS (बिज़नेस लॉजिक और API Endpoints के लिए)
3. **Database (Storage):** PostgreSQL (डेटा स्टोर और कॉन्फ्लिक्ट (Double Booking) रोकने के लिए)

---

## 🔥 2. सबसे अहम सवाल: Double Booking कैसे रोकी गई? (EXCLUDE USING gist)

इंटरव्यू में सबसे ज्यादा फोकस इसी पर होगा कि अगर 2 लोग एक ही समय (millisecond) पर एक ही कमरा (Resource) बुक करने की कोशिश करें, तो सिस्टम उसे कैसे रोकेगा? (इसे **Race Condition** कहते हैं)।

### ❌ गलत तरीका (SELECT then INSERT):
ज्यादातर लोग पहले डेटाबेस में चेक करते हैं कि स्लाट खाली है या नहीं (`SELECT * FROM bookings WHERE...`), और अगर खाली होता है तो बुक करते हैं (`INSERT INTO bookings...`)। 
**नुकसान:** जब तक पहला व्यक्ति `INSERT` करता है, उसी मिलीसेकंड में दूसरा व्यक्ति भी `SELECT` करके खाली देख सकता है, और दोनों का `INSERT` हो जाएगा (Double Booking हो जाएगी)।

### ✅ हमारा सही तरीका (EXCLUDE USING gist Constraint):
हमने सारा काम डेटाबेस (PostgreSQL) पर छोड़ दिया है। हमने `bookings` टेबल बनाते समय एक खास रूल (Constraint) लगाया है:

```sql
EXCLUDE USING gist (
  resource_id WITH =,
  tstzrange(start_time, end_time, '[)') WITH &&
)
```

**यह कैसे काम करता है?**
1. **`EXCLUDE USING gist`**: यह PostgreSQL का एक एडवांस्ड फीचर है जो यह पक्का करता है कि टेबल में कोई भी दो रो (row) आपस में ओवरलैप (Overlap) न करें।
2. **`resource_id WITH =`**: इसका मतलब है कि यह रूल सिर्फ तब लागू होगा जब दोनों बुकिंग का कमरा (resource_id) एक ही हो। (अगर कमरे अलग हैं, तो एक ही समय पर बुकिंग हो सकती है)।
3. **`tstzrange(start_time, end_time, '[)') WITH &&`**: 
   - `tstzrange` एक Time Range बनाता है (जैसे 10:00 से 11:00)।
   - `&&` ऑपरेटर का मतलब है "Overlap" (क्या दोनों समय टकरा रहे हैं?)।
   - `[)` का मतलब है: Start Time इन्क्लूड (Include) होगा, लेकिन End Time एक्सक्लूड (Exclude) होगा। यानी अगर एक मीटिंग 10:00 से 11:00 है, तो दूसरी मीटिंग ठीक 11:00 बजे शुरू हो सकती है, वो टकराएगी नहीं।

**फायदा:** अगर 100 लोग भी एक साथ `INSERT` करने की कोशिश करेंगे, तो PostgreSQL सिर्फ पहले वाले को एंट्री देगा, बाकी सबको **`23P01` (Exclusion Violation)** एरेर फेंक देगा। हमारा बैकएंड (NestJS) इस एरर को पकड़ कर यूज़र को "Slot already booked" (409 Conflict) का मैसेज दिखा देगा।

---

## 🌍 3. Timezones और Luxon Library (समय का मैनेजमेंट)

इंटरव्यूअर पूछेगा: **"अलग-अलग देशों (Timezones) का समय कैसे हैंडल किया है?"**

### नियम: डेटाबेस में हमेशा UTC Time!
हमने डेटाबेस में कभी भी किसी देश का लोकल समय (Wall-clock time) सेव नहीं किया है। `bookings` टेबल में समय हमेशा **UTC** (`TIMESTAMPTZ` डेटाटाइप) में सेव होता है।

### Luxon क्या है और क्यों इस्तेमाल किया?
Luxon जावास्क्रिप्ट की एक बेहतरीन लाइब्रेरी है जो समय और Timezones को मैनेज करती है (यह `moment.js` का नया और बेहतर रूप है)।

**हमने इसे कैसे इस्तेमाल किया?**
- हर रिसोर्स (कमरे) का अपना एक IANA Timezone होता है (जैसे `Europe/London` या `Asia/Kolkata`)।
- लंदन में काम करने का समय सुबह 9 से शाम 5 (09:00 - 17:00) है। लेकिन भारत से देखने पर वह समय अलग होगा।
- **Luxon** की मदद से हम लंदन के सुबह 9 बजे को पहले ग्‍लोबल UTC समय में बदलते हैं, और फिर डेटाबेस में चेक करते हैं।
- **DST (Daylight Saving Time):** कुछ देशों में गर्मियों और सर्दियों में घड़ी 1 घंटा आगे-पीछे होती है। Luxon IANA रूल्स के अनुसार यह अपने आप हैंडल कर लेता है।

---

## ⚙️ 4. Backend (NestJS) Deep Dive

### ORM (Prisma/TypeORM) क्यों नहीं इस्तेमाल किया?
हमने जानबूझ कर **raw `pg` pool (SQL)** का इस्तेमाल किया है। 
**कारण:** ORM (जैसे Prisma) में `EXCLUDE USING gist` जैसे एडवांस्ड PostgreSQL फीचर्स को सेटअप करना और इस्तेमाल करना बहुत मुश्किल होता है। Raw SQL लिखने से हमें डेटाबेस पर पूरा कंट्रोल मिलता है और परफॉरमेंस भी फ़ास्ट होती है।

### Folder Structure:
- `controllers/`: यहाँ API के एंडपॉइंट्स (`@Get`, `@Post`) होते हैं। यह सिर्फ यूज़र से रिक्वेस्ट लेता है और सर्विस को भेज देता है।
- `services/`: यहाँ असली बिज़नेस लॉजिक होता है। (जैसे Luxon से टाइम बदलना, डेटाबेस में क्वेरी चलाना)।
- `config/database.config.ts`: यहाँ Database Connection Pool बनता है।

---

## 💻 5. Frontend (React) Deep Dive

- **`useSlots.ts` (Custom Hook):** यह हुक रिएक्ट में डेट (Date) या रिसोर्स बदलने पर अपने आप बैकएंड से नए खाली स्लॉट्स मंगाता है।
- **Glassmorphism UI:** डिज़ाइन को प्रीमियम बनाने के लिए हमने पारदर्शी (Transparent) बैकग्राउंड और ब्लर (Blur) इफ़ेक्ट का इस्तेमाल किया है।
- **API Client (`client.ts`):** यह फाइल बैकएंड से जुड़ती है। Vercel पर लाइव होने पर यह `REACT_APP_API_URL` का इस्तेमाल करती है ताकि Render के लाइव डेटाबेस से बात कर सके।

---

## 🎯 6. Interview Q&A (संभावित प्रश्न और उत्तर)

**Q1: आपने Concurrency (एक साथ कई रिक्वेस्ट) को कैसे टेस्ट किया?**
> **Answer:** "मैंने बैकएंड में Jest का इस्तेमाल करके एक कॉनकरेंसी टेस्ट लिखा है। इस टेस्ट में मैंने एक साथ 10 API रिक्वेस्ट (Promises.all का यूज़ करके) एक ही स्लॉट बुक करने के लिए भेजी। रिजल्ट में हमेशा 1 रिक्वेस्ट पास (201 Created) हुई और बाकी 9 फेल (409 Conflict) हुईं। इससे साबित होता है कि हमारा सिस्टम 100% फुल-प्रूफ है।"

**Q2: NestJS को Express.js के ऊपर क्यों चुना?**
> **Answer:** "NestJS बाय डिफ़ॉल्ट एक बेहतरीन स्ट्रक्चर (Dependency Injection, Modules) देता है। Express.js में अगर प्रोजेक्ट बड़ा हो जाए तो फाइलें मैनेज करना मुश्किल हो जाता है, जबकि NestJS एंटरप्राइज-लेवल (बड़ी कंपनियों के) प्रोजेक्ट्स के लिए परफेक्ट है।"

**Q3: क्या होगा अगर बैकएंड चलते-चलते क्रैश हो जाए?**
> **Answer:** "मैंने ग्लोबल लेवल पर ट्राई-कैच (try-catch) एरर हैंडलिंग की है। अगर डेटाबेस डाउन भी हो जाता है, तो सर्वर क्रैश नहीं होगा बल्कि यूज़र को '500 Internal Server Error' का क्लीन रिस्पॉन्स मिलेगा। लाइव प्रोडक्शन में हम इसे PM2 या Docker रीस्टार्ट पॉलिसी से अपने आप रीस्टार्ट करवा सकते हैं।"

**Q4: आपने `tstzrange` में `[)` (Half-open interval) का इस्तेमाल क्यों किया?**
> **Answer:** "अगर मैं `[]` (Fully closed) का इस्तेमाल करता, तो 10:00-11:00 और 11:00-12:00 के स्लॉट आपस में टकरा (Overlap) जाते, क्योंकि दोनों में ठीक 11:00 बजे का समय शामिल होता। `[)` का मतलब है कि 11:00 बजे वाला समय पहले स्लॉट में शामिल नहीं है, इसलिए वे टकराएंगे नहीं और बैक-टू-बैक मीटिंग्स आराम से हो सकेंगी।"

---
**Best of luck!** इस डॉक्यूमेंट को 2-3 बार ध्यान से पढ़ें। अगर आप इंटरव्यूअर को `EXCLUDE USING gist` और `Luxon` वाला लॉजिक कॉन्फिडेंस के साथ समझा देंगे, तो आपका सिलेक्शन पक्का है!
